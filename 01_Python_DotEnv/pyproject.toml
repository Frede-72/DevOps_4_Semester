[project]
name = "01-python-dotenv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Frede-72", email = "frjo0005@stud.ek.dk" }
]
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.3",
]

[project.scripts]
01-python-dotenv = "01_python_dotenv:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
